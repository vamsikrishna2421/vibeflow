# VibeFlow runtime dependencies.
# Install with:  pip install -r requirements.txt
# (The provided installer script does this for you inside a virtual environment.)

faster-whisper>=1.0.0     # offline speech-to-text engine (CTranslate2)
sounddevice>=0.4.6        # microphone capture
numpy>=1.24              # audio buffers
pynput>=1.7.6            # global hotkeys + keyboard output
pyperclip>=1.8.2         # clipboard access
pystray>=0.19.5          # system tray icon
Pillow>=10.0            # tray icon image
PyYAML>=6.0             # human-friendly settings file

# Windows-only: reliable detection of the focused text field.
comtypes>=1.2.0 ; sys_platform == "win32"

# macOS-only: the menu-bar UI + the Cocoa / Accessibility / AVFoundation bridges
# the Mac "hands" use (focused-element role, frontmost app, mic-permission check).
# pynput already pulls in Cocoa/Quartz on macOS; these make the rest explicit.
rumps>=0.4.0 ; sys_platform == "darwin"
pyobjc-framework-Cocoa>=10.0 ; sys_platform == "darwin"
pyobjc-framework-Quartz>=10.0 ; sys_platform == "darwin"
pyobjc-framework-ApplicationServices>=10.0 ; sys_platform == "darwin"
pyobjc-framework-AVFoundation>=10.0 ; sys_platform == "darwin"

# Optional "Offline — no Ollama" AI tier: an in-process llama.cpp LLM.
# Bundled so the tier works on select; the model itself downloads on demand.
# The extra index serves prebuilt CPU wheels so installs never compile llama.cpp.
--extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cpu
llama-cpp-python>=0.3.0
